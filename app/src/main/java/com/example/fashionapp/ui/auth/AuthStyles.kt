package com.example.fashionapp.ui.auth

import androidx.compose.ui.graphics.Color

// NOTE: The auth/onboarding flow is intentionally light-themed only and lives
// outside FashionAppTheme / AppTheme.colors. These constants are fixed on purpose
// (the auth screens always render on a light background, regardless of dark mode).
// Do NOT swap them for MaterialTheme.colorScheme / AppTheme.colors.
val AuthPrimaryBlue = Color(0xFF1A56DB)
val AuthBackground = Color(0xFFF6F7FB)
val AuthTextDark = Color(0xFF1C1C1C)
val AuthTextSubtle = Color(0xFF7C7C7C)
